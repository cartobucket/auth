package com.cartobucket.auth.data.domain

enum class ResourceType {
    APPLICATIONS,
    AUTHORIZATION_SERVERS,
    APPLICATION_SECRETS,
    CLIENTS,
    SCHEMAS,
    SCOPES,
    TEMPLATES,
    USERS
}